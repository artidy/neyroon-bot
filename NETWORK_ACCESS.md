# Доступ к админ панели из локальной сети

## ✅ Настройка завершена!

Админ панель теперь доступна из локальной сети.

## 🌐 Доступные адреса

### С этого компьютера (localhost):
```
http://localhost:3000/admin/
```

### Из локальной сети:
```
http://192.168.1.67:3000/admin/
```

**Важно:** Используйте IP адрес `192.168.1.67` - это ваш основной сетевой адрес.

Другие IP адреса (192.168.139.1, 192.168.190.1, и т.д.) - это виртуальные адаптеры (WSL, Docker, VPN), они не будут работать из локальной сети.

## 📱 Доступ с телефона/планшета

1. Убедитесь, что устройство подключено к **той же Wi-Fi сети**
2. Откройте браузер на устройстве
3. Введите адрес: `http://192.168.1.67:3000/admin/`
4. Должна загрузиться админ панель

## 💻 Доступ с другого компьютера в сети

1. Другой компьютер должен быть в той же локальной сети
2. Откройте браузер
3. Введите: `http://192.168.1.67:3000/admin/`

## 🔒 Безопасность

**ВАЖНО:** Админ панель защищена токеном (ADMIN_SECRET), но доступна в локальной сети.

### Рекомендации для продакшена:

1. **Используйте HTTPS** - настройте SSL сертификат
2. **Firewall** - ограничьте доступ только к нужным IP
3. **VPN** - используйте VPN для удаленного доступа
4. **Сильный ADMIN_SECRET** - измените токен на длинный случайный

## 🔥 Firewall Windows

Если админ панель не открывается из сети, проверьте Windows Firewall:

### Вариант 1: Через PowerShell (от администратора)
```powershell
New-NetFirewallRule -DisplayName "Neyroon Bot Admin" -Direction Inbound -LocalPort 3000 -Protocol TCP -Action Allow
```

### Вариант 2: Через GUI
1. Откройте "Параметры Windows" → "Конфиденциальность и безопасность" → "Безопасность Windows"
2. Перейдите в "Брандмауэр и защита сети"
3. Нажмите "Дополнительные параметры"
4. Выберите "Правила для входящих подключений"
5. Нажмите "Создать правило"
6. Выберите "Для порта" → TCP → 3000
7. Разрешите подключение
8. Примените ко всем профилям
9. Назовите правило "Neyroon Bot Admin"

## 🧪 Проверка доступа

### С этого компьютера:
```cmd
curl http://localhost:3000/health
```

### Из локальной сети (с другого устройства):
```
curl http://192.168.1.67:3000/health
```

Должен вернуться ответ:
```json
{"status":"ok","timestamp":"2025-11-30T..."}
```

## 📊 Узнать текущий IP адрес

Если IP адрес изменился (например, после перезагрузки роутера):

### Windows CMD:
```cmd
ipconfig | findstr "IPv4"
```

### PowerShell:
```powershell
Get-NetIPAddress -AddressFamily IPv4 | Where-Object {$_.InterfaceAlias -notlike "*Loopback*" -and $_.InterfaceAlias -notlike "*WSL*"} | Select-Object IPAddress
```

Ищите адрес вида `192.168.x.x` или `10.x.x.x`

## 🚀 Автоматическое определение IP

Бот теперь автоматически показывает сетевой IP при запуске в логах:

```
✅ Admin server running on http://0.0.0.0:3000
   Local: http://localhost:3000
   Network: http://192.168.1.67:3000
```

Проверьте логи командой:
```cmd
docker logs neyroon-bot-dev | findstr "Network"
```

## 🌍 Доступ через интернет (ngrok)

Для доступа из интернета используйте ngrok:

1. Установите ngrok: https://ngrok.com/download
2. Запустите: `ngrok http 3000`
3. Скопируйте публичный URL (например: https://abc123.ngrok.app)
4. Используйте этот URL для доступа к админ-панели из интернета

**Внимание:** ngrok подходит только для тестирования! Для продакшена используйте выделенный сервер.

## ❓ Troubleshooting

### Проблема: "Не удается получить доступ к сайту"
**Решение:**
1. Проверьте, что бот запущен: `docker ps | findstr neyroon`
2. Проверьте firewall (см. выше)
3. Убедитесь, что устройства в одной сети
4. Попробуйте другой IP адрес из списка `ipconfig`

### Проблема: Долго грузится
**Решение:**
- Проверьте антивирус - он может блокировать соединения
- Отключите VPN на обоих устройствах

### Проблема: 401 Unauthorized при вызове API
**Решение:**
- Это нормально! HTML страница `/admin/` должна открываться без авторизации
- API endpoints требуют токен ADMIN_SECRET
- В админ панели введите токен из `.env` файла

## 📝 Полезные ссылки

- **Админ панель:** http://192.168.1.67:3000/admin/
- **API Health:** http://192.168.1.67:3000/health
- **Загруженные файлы:** http://192.168.1.67:3000/uploads/

---

✅ **Админ панель готова к использованию из локальной сети!**
