# Процесс для новичков

## 📋 Обзор

После нажатия кнопки "Я новичок" пользователь видит список бесплатных уроков с кнопкой "Хочу в проект".

## 🎯 Логика работы

### Шаг 1: Выбор уровня опыта

Пользователь нажимает `/start` и видит:
- Приветственное сообщение
- Две кнопки:
  - **"Я новичок"**
  - **"Уже знаком"**

### Шаг 2: Новичок выбирает "Я новичок"

После нажатия кнопки "Я новичок":

1. **Показывается список FREE уроков:**
   ```
   📚 Бесплатные уроки:

   1. Основы композиции
      Изучаем правило третей и золотое сечение

   2. Работа с цветом
      Основы цветоведения

   [Кнопка: 🚀 Хочу в проект]
   ```

2. **Пользователь видит:**
   - Список всех активных уроков типа `FREE`
   - Название каждого урока
   - Описание (если есть)
   - Кнопку "🚀 Хочу в проект" снизу списка

### Шаг 3: Нажатие кнопки "Хочу в проект"

После нажатия кнопки "Хочу в проект":
- Пользователь переходит к процессу оформления подписки
- Показывается информация о стоимости и условиях
- Предлагается выбрать способ оплаты

## 🎨 Типы уроков

### FREE (Бесплатный)
- Доступен всем новичкам
- Показывается в списке сразу после выбора "Я новичок"
- Можно добавить несколько FREE уроков для демонстрации качества

### PREMIUM (Премиум)
- Требует активную подписку
- Доставляется ежедневно по расписанию
- Основной контент курса

## ⚠️ Важные изменения

### Удалены типы уроков:
- ❌ **INTRO** - больше не используется
- ❌ **FINAL** - больше не используется

### Новая логика:
- ✅ После выбора "Новичок" показывается **список** FREE уроков (не один урок)
- ✅ Кнопка "Хочу в проект" ведет к оформлению подписки
- ✅ Только два типа уроков: FREE и PREMIUM

## 🔧 Техническая реализация

### Код в `src/bot/handlers/start.ts`:

```typescript
if (isNewbie) {
  // Получаем все бесплатные уроки
  const freeLessons = await lessonService.getLessonsByType('FREE');

  if (freeLessons && freeLessons.length > 0) {
    // Формируем список бесплатных уроков
    let lessonsList = '📚 *Бесплатные уроки:*\n\n';

    freeLessons.forEach((lesson, index) => {
      lessonsList += `${index + 1}. ${lesson.title}\n`;
      if (lesson.description) {
        lessonsList += `   _${lesson.description}_\n`;
      }
      lessonsList += '\n';
    });

    // Добавляем кнопку "Хочу в проект" снизу списка
    const keyboard = new InlineKeyboard()
      .text('🚀 Хочу в проект', 'want_to_join_project');

    await ctx.reply(lessonsList, {
      parse_mode: 'Markdown',
      reply_markup: keyboard,
    });
  }
}
```

### Функция в `src/services/lessonService.ts`:

```typescript
async getLessonsByType(type: LessonType) {
  return await prisma.lesson.findMany({
    where: { type, isActive: true },
    orderBy: { order: 'asc' },
    include: {
      videos: {
        orderBy: { order: 'asc' },
      },
    },
  });
}
```

## 📝 Добавление бесплатных уроков

### Через админ панель:

1. Откройте [http://localhost:3000/admin/](http://localhost:3000/admin/)
2. Перейдите на вкладку **"📚 Уроки"**
3. Нажмите **"➕ Добавить урок"**
4. Заполните форму:
   - **Номер урока:** 1 (или другой свободный номер)
   - **Тип урока:** FREE
   - **Название:** "Основы композиции"
   - **Описание:** "Изучаем правило третей"
   - **Порядок отображения:** 1
   - **Урок активен:** ✓
5. Нажмите **"💾 Сохранить"**

Повторите для создания нескольких FREE уроков (рекомендуется 2-3 урока).

## 🧪 Тестирование

### Тест 1: Проверка списка FREE уроков

1. Создайте несколько уроков типа FREE через админ панель
2. Отправьте `/start` боту
3. Нажмите "Я новичок"
4. **Ожидается:**
   - Список всех FREE уроков с названиями
   - Описания под названиями (если указаны)
   - Кнопка "🚀 Хочу в проект" снизу

### Тест 2: Пустой список

1. Убедитесь, что нет активных уроков типа FREE
2. Отправьте `/start` и нажмите "Я новичок"
3. **Ожидается:**
   - Сообщение "К сожалению, бесплатные уроки пока недоступны."

### Тест 3: Кнопка "Хочу в проект"

1. Нажмите кнопку "🚀 Хочу в проект"
2. **Ожидается:**
   - Переход к процессу оформления подписки

## 📊 Сравнение: До и После

### До изменений:
```
Новичок → Один FREE урок с видео → Выбор времени
```

### После изменений:
```
Новичок → Список FREE уроков → Кнопка "Хочу в проект" → Оформление подписки
```

## 🎯 Преимущества новой логики

1. **Больше демонстрации качества** - можно показать несколько бесплатных уроков
2. **Четкий призыв к действию** - кнопка "Хочу в проект"
3. **Упрощенная структура** - только FREE и PREMIUM типы
4. **Гибкость** - можно добавлять/удалять FREE уроки через админ панель

---

✅ **Новая логика для новичков готова!**
