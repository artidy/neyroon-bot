# Меню бота в Telegram

## Обзор

В боте настроено постоянное меню команд, которое отображается в Telegram интерфейсе. Пользователи могут видеть доступные команды, нажав на кнопку меню (☰) рядом с полем ввода сообщения.

## Доступные команды

### 🏠 `/start` - Начать / Главное меню
Запускает бота и показывает главное меню. Если пользователь новый, начинается процесс онбординга.

**Использование:**
```
/start
```

**Что происходит:**
- Новые пользователи: начинается онбординг (выбор уровня опыта)
- Существующие пользователи: показывается главное меню с опциями

### 📋 `/menu` - Открыть главное меню
Показывает главное меню бота с доступными опциями.

**Использование:**
```
/menu
```

**Доступные опции в меню:**
- 💳 Оплатить подписку
- ⏰ Изменить время уроков
- 📊 Мой прогресс
- ℹ️ О боте
- 💬 Связаться с нами

### 🧪 `/test_payment` - Тестовая оплата (dev)
**⚠️ Только для разработки!**

Позволяет симулировать успешную оплату подписки без реальной транзакции.

**Использование:**
```
/test_payment
```

**Требования:**
1. У вас должна быть pending подписка
2. Бот должен быть запущен в режиме разработки

**Процесс:**
1. Команда находит вашу pending подписку
2. Показывает кнопки выбора платежной системы
3. При нажатии - симулирует успешную оплату
4. Активирует подписку
5. Предлагает выбрать время для уроков

**Подробнее:** См. [TELEGRAM_TEST_PAYMENT.md](TELEGRAM_TEST_PAYMENT.md)

### 🔄 `/reset` - Сбросить прогресс (dev)
**⚠️ Только для разработки!**

Сбрасывает статус онбординга и позволяет пройти его заново.

**Использование:**
```
/reset
```

**Что происходит:**
- Сбрасывается `onboardingCompleted` в `false`
- Можно заново пройти выбор уровня опыта
- Не удаляет подписки и другие данные

## Как настроено

Меню команд настроено в файле `src/bot/index.ts`:

```typescript
bot.api.setMyCommands([
  { command: 'start', description: '🏠 Начать / Главное меню' },
  { command: 'menu', description: '📋 Открыть главное меню' },
  { command: 'test_payment', description: '🧪 Тестовая оплата (dev)' },
  { command: 'reset', description: '🔄 Сбросить прогресс (dev)' },
]).catch((err) => {
  console.error('Error setting bot commands:', err);
});
```

## Для production

Перед деплоем в production удалите команды для разработки:

```typescript
// Production версия
bot.api.setMyCommands([
  { command: 'start', description: '🏠 Начать / Главное меню' },
  { command: 'menu', description: '📋 Открыть главное меню' },
]).catch((err) => {
  console.error('Error setting bot commands:', err);
});
```

**ИЛИ** добавьте проверку окружения:

```typescript
const commands = [
  { command: 'start', description: '🏠 Начать / Главное меню' },
  { command: 'menu', description: '📋 Открыть главное меню' },
];

// Добавляем dev команды только в разработке
if (process.env.NODE_ENV === 'development') {
  commands.push(
    { command: 'test_payment', description: '🧪 Тестовая оплата (dev)' },
    { command: 'reset', description: '🔄 Сбросить прогресс (dev)' }
  );
}

bot.api.setMyCommands(commands).catch((err) => {
  console.error('Error setting bot commands:', err);
});
```

## Как пользователи видят меню

1. **В мобильном Telegram:**
   - Нажмите на кнопку меню (☰) слева от поля ввода
   - Увидите список команд с описаниями
   - Нажмите на команду для выполнения

2. **В desktop Telegram:**
   - Кнопка меню (/) в правом нижнем углу поля ввода
   - ИЛИ начните вводить `/` в поле ввода
   - Появится список доступных команд

3. **Автодополнение:**
   - Начните вводить `/` и начало команды
   - Telegram автоматически предложит совпадающие команды

## Обновление меню

Чтобы изменить меню команд:

1. Отредактируйте массив в `bot.api.setMyCommands()`
2. Пересоберите бот: `npm run build`
3. Перезапустите: `docker restart neyroon-bot-dev`
4. Меню обновится автоматически для всех пользователей

## Ограничения

- **Максимум 100 команд** на бота
- **Описание не более 256 символов**
- **Команда не более 32 символов**
- Команды должны содержать только `a-z`, `0-9` и `_`

## Дополнительные ресурсы

- [Telegram Bot API - setMyCommands](https://core.telegram.org/bots/api#setmycommands)
- [Grammy Documentation - Commands](https://grammy.dev/guide/commands.html)
- [TELEGRAM_TEST_PAYMENT.md](TELEGRAM_TEST_PAYMENT.md) - Подробнее о `/test_payment`

---

✅ **Меню команд настроено и работает!**
