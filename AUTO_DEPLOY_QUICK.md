# Автоматическое развертывание - Быстрая настройка

## За 10 минут настроите автодеплой! 🚀

### Что получите:
- ✅ `git push` → автоматическое обновление на сервере
- ✅ Уведомления в Telegram о деплое
- ✅ Проверка работоспособности после деплоя
- ✅ Откат в случае ошибки

---

## Шаг 1: Настройка сервера (2 минуты)

```bash
# 1. Подключитесь к серверу
ssh root@YOUR_SERVER_IP

# 2. Создайте SSH ключ
ssh-keygen -t ed25519 -C "github-actions" -N ""

# 3. Добавьте в authorized_keys
cat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys

# 4. Скопируйте приватный ключ (весь вывод!)
cat ~/.ssh/id_ed25519
```

**Сохраните вывод последней команды** - это ваш SSH ключ для GitHub.

---

## Шаг 2: Настройка GitHub Secrets (3 минуты)

Перейдите: **GitHub** → **Ваш репозиторий** → **Settings** → **Secrets and variables** → **Actions**

Добавьте 4 секрета:

| Имя | Значение | Пример |
|-----|----------|--------|
| `VPS_HOST` | IP адрес сервера | `192.168.1.100` |
| `VPS_USERNAME` | Имя пользователя SSH | `root` |
| `VPS_SSH_KEY` | Приватный SSH ключ | `-----BEGIN OPENSSH...` |
| `VPS_PORT` | SSH порт | `22` |

**Для уведомлений в Telegram (опционально):**

| Имя | Значение | Где взять |
|-----|----------|-----------|
| `TELEGRAM_BOT_TOKEN` | Токен бота | Тот же, что `BOT_TOKEN` |
| `TELEGRAM_ADMIN_ID` | Ваш Telegram ID | [@userinfobot](https://t.me/userinfobot) |

---

## Шаг 3: Выбор варианта деплоя (1 минута)

### Вариант A: Простой (рекомендуется для начала)

Используйте `.github/workflows/deploy.yml` - уже активен!

```bash
# Ничего делать не нужно, уже настроено
```

### Вариант B: С уведомлениями в Telegram

```bash
# Переименуйте файлы
mv .github/workflows/deploy.yml .github/workflows/deploy-simple.yml
mv .github/workflows/deploy-with-notifications.yml .github/workflows/deploy.yml

# Закоммитьте
git add .
git commit -m "Enable Telegram notifications for deployments"
git push
```

---

## Шаг 4: Первый автодеплой! (2 минуты)

```bash
# 1. Внесите любое изменение
echo "# Auto-deploy configured!" >> README.md

# 2. Закоммитьте и запушьте
git add README.md
git commit -m "Test: Auto-deployment setup"
git push origin main
```

### Проверьте деплой:

1. Откройте GitHub → вкладка **Actions**
2. Увидите запущенный workflow "Deploy to VPS"
3. Нажмите на него для просмотра логов

**Время деплоя: ~2-3 минуты**

---

## Шаг 5: Проверка работы (2 минуты)

### На сервере:

```bash
ssh root@YOUR_SERVER_IP

cd /opt/neyroon-bot

# Проверьте, что изменения применились
cat README.md  # Должно быть "# Auto-deploy configured!"

# Проверьте контейнеры
docker-compose -f docker-compose.prod.yml ps

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs --tail 20 bot
```

### В Telegram (если настроили):

Вы получите сообщение:
```
✅ Развертывание успешно завершено!

Проект: Neyroon Bot
Коммит: Test: Auto-deployment setup
Время: ...
```

---

## Готово! 🎉

Теперь при каждом `git push` ваш проект автоматически обновится на сервере!

### Что происходит при `git push`:

```
1. GitHub получает ваш push
   ↓
2. Запускается GitHub Actions workflow
   ↓
3. Подключение к серверу по SSH
   ↓
4. git pull (загрузка изменений)
   ↓
5. docker-compose down (остановка)
   ↓
6. docker-compose build (пересборка)
   ↓
7. docker-compose up -d (запуск)
   ↓
8. Health check (проверка работы)
   ↓
9. ✅ Успешно! / ❌ Ошибка
   ↓
10. Уведомление в Telegram
```

---

## Использование

### Обычный workflow:

```bash
# 1. Вносите изменения локально
nano src/bot/messages.ts

# 2. Коммитите
git add .
git commit -m "Update welcome message"

# 3. Пушите - деплой запустится автоматически!
git push origin main

# 4. Ждете 2-3 минуты

# 5. Проверяете в Telegram или на сайте
```

### Проверка статуса деплоя:

- **GitHub Actions** → видите прогресс в реальном времени
- **Telegram** → получаете уведомление
- **Сервер** → `docker-compose logs -f bot`

---

## Troubleshooting

### ❌ Ошибка: "Permission denied"

```bash
# На сервере
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### ❌ Ошибка: "git pull failed"

```bash
# На сервере
cd /opt/neyroon-bot
git config --global --add safe.directory /opt/neyroon-bot
git config pull.rebase false
```

### ❌ Деплой зависает

Проверьте логи в GitHub Actions - там будет видно на каком этапе проблема.

### ❌ Уведомления не приходят

Проверьте, что:
1. `TELEGRAM_BOT_TOKEN` и `TELEGRAM_ADMIN_ID` добавлены в GitHub Secrets
2. Используете workflow `deploy-with-notifications.yml`

---

## Расширенные возможности

### Деплой только из production ветки

Измените в `.github/workflows/deploy.yml`:

```yaml
on:
  push:
    branches:
      - production  # Только из production
```

Тогда:
```bash
# Работаете в main
git checkout main
# Делаете изменения
git add .
git commit -m "New feature"
git push origin main  # Деплой НЕ запустится

# Когда готовы к деплою:
git push origin main:production  # Деплой запустится!
```

### Деплой только при изменении кода

```yaml
on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'prisma/**'
      - 'package.json'
```

Теперь деплой запустится только если изменились эти файлы.

### Ручной деплой из GitHub UI

Добавьте в workflow:
```yaml
on:
  push:
    branches:
      - main
  workflow_dispatch:  # Позволяет запустить вручную
```

Теперь можете запустить деплой вручную: **GitHub** → **Actions** → **Deploy to VPS** → **Run workflow**

---

## Полезные ссылки

- 📖 Полная документация: [CI_CD_SETUP.md](CI_CD_SETUP.md)
- 🐳 Вариант с Docker Hub: [.github/workflows/deploy-docker-hub.yml](.github/workflows/deploy-docker-hub.yml)
- 🚀 Деплой на VPS: [QUICK_DEPLOY.md](QUICK_DEPLOY.md)
- ✅ Чеклист перед деплоем: [PRODUCTION_CHECKLIST.md](PRODUCTION_CHECKLIST.md)

---

**Вопросы?** Проверьте [CI_CD_SETUP.md](CI_CD_SETUP.md) для детальной инструкции.
