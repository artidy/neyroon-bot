# Интеграция с Prodamus

## Обзор

Prodamus - современная платежная система для приема онлайн-платежей в РФ и Казахстане. Поддерживает карты Visa, Mastercard, МИР, а также Kaspi Gold и Halyk Bank.

## Преимущества Prodamus

✅ **Работает в Казахстане** - принимает карты Kaspi Gold, Halyk Bank
✅ **Низкий порог входа** - можно начать как физлицо/самозанятый
✅ **Простая интеграция** - готовый API
✅ **Низкая комиссия** - от 2.8%
✅ **Быстрая регистрация** - 1-2 дня
✅ **Telegram Payments** - встроенная оплата в Telegram (опционально)

## Настройка

### 1. Регистрация в Prodamus

1. Зарегистрируйтесь на [prodamus.ru](https://prodamus.ru)
2. Выберите тариф (есть бесплатный для старта)
3. Пройдите верификацию (нужен ИНН или ИП)
4. Получите доступ к личному кабинету

### 2. Получение учетных данных

В личном кабинете Prodamus найдите:
- **Project ID** - идентификатор вашего проекта (например: `myproject`)
- **API Key** - ключ для API запросов
- **Secret Key** - секретный ключ для проверки подписей

### 3. Настройка переменных окружения

Добавьте в `.env` файл:

```env
# Prodamus (РФ + Казахстан)
PRODAMUS_API_KEY=ваш_api_key
PRODAMUS_SECRET_KEY=ваш_secret_key
PRODAMUS_PROJECT_ID=myproject

# Bot Settings
BOT_USERNAME=ваш_bot_username
WEBHOOK_URL=https://ваш-домен.com
```

### 4. Настройка Webhook в Prodamus

В личном кабинете Prodamus укажите URL для получения уведомлений:

```
https://ваш-домен.com/api/payments/prodamus/webhook
```

## Как это работает

### Процесс оплаты

1. **Создание платежа**
   - Пользователь нажимает "Оплатить через Prodamus"
   - Бот создает запись о подписке со статусом `pending`
   - Отправляется запрос к Prodamus API
   - Пользователь получает ссылку на оплату

2. **Оплата**
   - Пользователь переходит по ссылке
   - Оплачивает картой Visa/Mastercard/МИР/Kaspi Gold/Halyk Bank
   - Prodamus обрабатывает платеж

3. **Подтверждение**
   - Prodamus отправляет webhook на ваш сервер
   - Сервер обновляет статус подписки на `completed`
   - Активируется доступ к урокам
   - Бот отправляет уведомление пользователю
   - Пользователь выбирает время для получения уроков

### Основные файлы

#### [src/services/paymentService.ts](src/services/paymentService.ts#L119-L186)
```typescript
async createProdamusPaymentLink(subscriptionId: string): Promise<string>
```
Создает платежную ссылку через Prodamus API.

#### [src/admin/server.ts](src/admin/server.ts#L387-L438)
```typescript
POST /api/payments/prodamus/webhook
```
Webhook endpoint для приема уведомлений от Prodamus.

#### [src/bot/index.ts](src/bot/index.ts#L178-L219)
```typescript
bot.callbackQuery('payment_prodamus', ...)
```
Обработчик кнопки "Оплатить через Prodamus".

## API Endpoints

### Webhook от Prodamus
```
POST /api/payments/prodamus/webhook
```

**Тело запроса (от Prodamus):**
```json
{
  "order_id": "subscription-uuid",
  "order_num": "12345",
  "payment_status": "success",
  "customer_phone": "123456789",
  "sum": "4000",
  "currency": "RUB"
}
```

**Статусы платежа:**
- `success` / `paid` - платеж успешен
- `failed` / `cancelled` - платеж не прошел
- `pending` / `processing` - платеж в обработке

### Проверка статуса платежа (вручную)
```
GET /api/payments/pending
Authorization: Bearer ADMIN_SECRET
```

Возвращает все ожидающие подтверждения платежи.

## Тестирование

### Локальное тестирование webhook

Для локального тестирования используйте [ngrok](https://ngrok.com/):

```bash
# Запустите ngrok для проброса локального порта
ngrok http 3000

# Используйте полученный URL в настройках webhook Prodamus
https://your-ngrok-url.ngrok.io/api/payments/prodamus/webhook
```

### Тестовые карты Prodamus

Prodamus предоставляет тестовые карты для разработки:

- **Успешная оплата**: `5555 5555 5555 4444`
- **Отклонен банком**: `5555 5555 5555 4477`
- Любая дата в будущем, любой CVV

### Тестовый платеж

1. Запустите бота в тестовом режиме
2. Нажмите "Оплатить подписку"
3. Выберите "Prodamus"
4. Используйте тестовую карту `5555 5555 5555 4444`
5. Проверьте, что webhook был получен
6. Убедитесь, что подписка активирована

## Безопасность

### Проверка подписи webhook (TODO)

В текущей версии закомментирована проверка подписи запросов от Prodamus.
Рекомендуется реализовать для production:

```typescript
// В server.ts, строка 400-403
const signature = req.headers['sign'];
if (!verifyProdamusSignature(req.body, signature)) {
  return res.status(401).json({ error: 'Invalid signature' });
}
```

Функция проверки подписи:

```typescript
function verifyProdamusSignature(data: any, signature: string): boolean {
  const crypto = require('crypto');
  const secretKey = process.env.PRODAMUS_SECRET_KEY;

  // Создаем строку для подписи (согласно документации Prodamus)
  const signString = Object.keys(data)
    .sort()
    .map(key => `${key}=${data[key]}`)
    .join(';');

  const hash = crypto
    .createHmac('sha256', secretKey)
    .update(signString)
    .digest('hex');

  return hash === signature;
}
```

### Рекомендации по безопасности

1. Всегда проверяйте подпись webhook-запросов
2. Используйте HTTPS для production
3. Храните `PRODAMUS_SECRET_KEY` в безопасности
4. Не выставляйте admin endpoints без авторизации
5. Логируйте все платежные операции

## Troubleshooting

### Webhook не приходит

1. Проверьте правильность URL в настройках Prodamus
2. Убедитесь, что URL доступен из интернета (не localhost)
3. Проверьте логи сервера на наличие ошибок
4. Используйте ngrok для локального тестирования
5. **Решение**: Используйте ручное подтверждение через админ-панель

### Платеж не подтверждается

1. Проверьте логи webhook-запросов
2. Убедитесь, что статус от Prodamus = "success" или "paid"
3. Проверьте, что order_id соответствует subscriptionId в БД
4. Проверьте, что БД была обновлена
5. **Решение**: Используйте ручное подтверждение через админ-панель

### Пользователь не получает уведомление

1. Проверьте, что бот запущен и работает
2. Убедитесь, что `setBotInstance()` вызывается при старте
3. Проверьте, что telegramId корректный
4. Посмотрите логи на наличие ошибок при отправке

### Ошибка "Invalid API Key"

1. Проверьте правильность `PRODAMUS_API_KEY` в `.env`
2. Убедитесь, что ключ активен в личном кабинете Prodamus
3. Проверьте, что проект активен

## API Reference Prodamus

Документация API Prodamus: https://help.prodamus.ru/api

### Создание платежа
```bash
POST https://{PROJECT_ID}.pay.prodamus.ru/api/orders/create
Authorization: Bearer {API_KEY}

{
  "order_id": "unique-order-id",
  "customer_phone": "79001234567",
  "products": [
    {
      "name": "Подписка на курс",
      "price": 4000,
      "quantity": 1,
      "sum": 4000
    }
  ],
  "do": "link",
  "urlSuccess": "https://t.me/yourbot",
  "urlNotification": "https://yourdomain.com/webhook"
}
```

**Ответ:**
```json
{
  "id": "12345",
  "payment_link": "https://myproject.pay.prodamus.ru/pl/12345",
  "status": "pending"
}
```

## Поддерживаемые способы оплаты

### Россия
- 💳 Visa
- 💳 Mastercard
- 💳 МИР
- 💰 ЮMoney
- 🏦 СБП (Система Быстрых Платежей)

### Казахстан
- 💳 Kaspi Gold
- 💳 Halyk Bank
- 💳 Visa/Mastercard международные

## Комиссии

Тарифы Prodamus (актуальные на 2024):

- **Старт**: 0₽/мес + 3.5% с платежа
- **Базовый**: 1500₽/мес + 2.8% с платежа
- **Оптимальный**: 4500₽/мес + 2.5% с платежа

## Дальнейшие улучшения

- [ ] Добавить проверку подписи webhook
- [ ] Реализовать автоматический возврат средств (refund)
- [ ] Добавить логирование всех платежных операций
- [ ] Реализовать повторные попытки при ошибках API
- [ ] Поддержка рекуррентных платежей (автопродление)
- [ ] Интеграция с Telegram Payments для оплаты внутри бота
- [ ] Поддержка разных тарифов подписки

## Telegram Payments (опционально)

Prodamus поддерживает Telegram Payments - это позволяет принимать оплату прямо в Telegram без перехода на внешний сайт.

### Настройка Telegram Payments

1. Напишите [@BotFather](https://t.me/BotFather)
2. Выберите вашего бота
3. Выберите "Payments" → "Prodamus"
4. Введите ваш Prodamus API Token
5. Готово! Теперь можно принимать оплату в Telegram

Это будет следующий этап интеграции.

## Поддержка

По вопросам интеграции с Prodamus:
- Документация: https://help.prodamus.ru
- Поддержка: support@prodamus.ru
- Telegram: @prodamus_support
