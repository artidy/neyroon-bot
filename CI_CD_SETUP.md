# Настройка CI/CD (Автоматическое развертывание)

## Что это дает?

После настройки CI/CD, при каждом `git push` в GitHub:
1. GitHub Actions автоматически подключится к вашему серверу
2. Загрузит последние изменения
3. Пересоберет Docker контейнеры
4. Перезапустит приложение
5. Проверит работоспособность
6. Отправит уведомление в Telegram (опционально)

**Время развертывания: ~2-3 минуты**

---

## Шаг 1: Подготовка сервера

### 1.1. Создайте SSH ключ на сервере (если еще нет)

```bash
# На сервере
ssh-keygen -t ed25519 -C "github-actions"

# Нажмите Enter 3 раза (без пароля для автоматизации)
# Ключ будет создан в ~/.ssh/id_ed25519
```

### 1.2. Добавьте публичный ключ в authorized_keys

```bash
cat ~/.ssh/id_ed25519.pub >> ~/.ssh/authorized_keys
```

### 1.3. Скопируйте приватный ключ

```bash
cat ~/.ssh/id_ed25519
```

Скопируйте весь вывод (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)

### 1.4. Инициализируйте Git репозиторий на сервере

```bash
cd /opt/neyroon-bot

# Если еще нет git репозитория
git init
git remote add origin https://github.com/YOUR_USERNAME/neyroon-bot.git

# Или если уже есть
git remote -v  # проверить
```

---

## Шаг 2: Настройка GitHub Secrets

### 2.1. Перейдите в настройки репозитория

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** (Настройки)
3. Выберите **Secrets and variables** → **Actions**
4. Нажмите **New repository secret**

### 2.2. Добавьте следующие секреты:

#### `VPS_HOST`
- **Name:** `VPS_HOST`
- **Value:** IP адрес вашего сервера (например: `192.168.1.100`)

#### `VPS_USERNAME`
- **Name:** `VPS_USERNAME`
- **Value:** Имя пользователя для SSH (обычно `root`)

#### `VPS_SSH_KEY`
- **Name:** `VPS_SSH_KEY`
- **Value:** Приватный SSH ключ (скопированный из `~/.ssh/id_ed25519`)

**Важно:** Вставьте весь ключ целиком, включая:
```
-----BEGIN OPENSSH PRIVATE KEY-----
...
-----END OPENSSH PRIVATE KEY-----
```

#### `VPS_PORT`
- **Name:** `VPS_PORT`
- **Value:** SSH порт (обычно `22`, или ваш кастомный порт)

---

## Шаг 3: Настройка уведомлений в Telegram (опционально)

Если хотите получать уведомления о деплое в Telegram:

### 3.1. Получите ваш Telegram ID

1. Напишите боту [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш ID (например: `123456789`)

### 3.2. Добавьте секреты в GitHub

#### `TELEGRAM_BOT_TOKEN`
- **Name:** `TELEGRAM_BOT_TOKEN`
- **Value:** Токен вашего бота (тот же, что в `BOT_TOKEN`)

#### `TELEGRAM_ADMIN_ID`
- **Name:** `TELEGRAM_ADMIN_ID`
- **Value:** Ваш Telegram ID (из @userinfobot)

---

## Шаг 4: Выбор workflow файла

В проекте есть два варианта:

### Вариант 1: Простой деплой (без уведомлений)

Используйте файл: `.github/workflows/deploy.yml`

Активен по умолчанию.

### Вариант 2: Деплой с уведомлениями в Telegram

Используйте файл: `.github/workflows/deploy-with-notifications.yml`

Чтобы активировать его:
```bash
# Переименуйте файлы
mv .github/workflows/deploy.yml .github/workflows/deploy.yml.disabled
mv .github/workflows/deploy-with-notifications.yml .github/workflows/deploy.yml
```

Или просто удалите ненужный файл.

---

## Шаг 5: Первый деплой

### 5.1. Закоммитьте изменения

```bash
git add .
git commit -m "Add CI/CD workflow"
git push origin main
```

### 5.2. Проверьте статус деплоя

1. Перейдите в ваш репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Вы увидите запущенный workflow "Deploy to VPS"
4. Нажмите на него, чтобы увидеть логи в реальном времени

### 5.3. Дождитесь завершения

- ✅ Зеленая галочка = успешный деплой
- ❌ Красный крестик = ошибка (проверьте логи)

---

## Как это работает?

```
Вы делаете:                GitHub Actions:              Ваш сервер:
─────────────             ─────────────────             ────────────

git add .                 1. Получает изменения
git commit -m "..."       2. Запускает workflow
git push origin main   →  3. Подключается к серверу →   4. git pull
                          5. Выполняет команды    →      5. docker-compose down
                          6. Проверяет health     →      6. docker-compose build
                          7. Отправляет уведомление      7. docker-compose up -d
                                                         8. Health check
```

---

## Тестирование

### Простой тест:

1. Внесите небольшое изменение в проект:
```bash
echo "# Test CI/CD" >> TEST.md
git add TEST.md
git commit -m "Test: CI/CD deployment"
git push origin main
```

2. Перейдите в GitHub Actions и наблюдайте за процессом

3. После завершения проверьте, что изменения на сервере:
```bash
ssh root@YOUR_SERVER_IP
cd /opt/neyroon-bot
cat TEST.md  # Должен появиться файл
```

---

## Расширенная настройка

### Деплой только при изменении определенных файлов

Отредактируйте `.github/workflows/deploy.yml`:

```yaml
on:
  push:
    branches:
      - main
    paths:
      - 'src/**'
      - 'prisma/**'
      - 'package.json'
      - 'Dockerfile'
      - 'docker-compose.prod.yml'
```

Теперь деплой будет запускаться только при изменении указанных файлов.

### Деплой только из production ветки

```yaml
on:
  push:
    branches:
      - production  # Только из production ветки
```

Тогда ваш workflow:
1. Работайте в ветке `main` / `dev`
2. Когда готовы к деплою: `git push origin main:production`
3. Автоматический деплой запустится только для production

### Добавление тестов перед деплоем

```yaml
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run tests
        run: |
          npm install
          npm test

  deploy:
    needs: test  # Деплой только если тесты прошли
    runs-on: ubuntu-latest
    # ... остальное как раньше
```

---

## Безопасность

### ✅ Рекомендации:

1. **Никогда не коммитьте секреты** - используйте GitHub Secrets
2. **Используйте SSH ключи** вместо паролей
3. **Ограничьте доступ к SSH ключу** только для GitHub Actions
4. **Регулярно обновляйте зависимости** в workflows

### ⚠️ Важно:

- Не храните `.env` файл в git
- Добавьте `.env` в `.gitignore`
- Приватный SSH ключ храните только в GitHub Secrets

---

## Troubleshooting

### Ошибка: "Permission denied (publickey)"

**Решение:**
```bash
# На сервере проверьте права
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# Проверьте, что ключ добавлен
cat ~/.ssh/authorized_keys
```

### Ошибка: "Host key verification failed"

**Решение:** Добавьте в workflow:
```yaml
- name: Deploy to VPS via SSH
  uses: appleboy/ssh-action@v1.0.0
  with:
    host: ${{ secrets.VPS_HOST }}
    username: ${{ secrets.VPS_USERNAME }}
    key: ${{ secrets.VPS_SSH_KEY }}
    port: ${{ secrets.VPS_PORT }}
    insecure: true  # Добавьте эту строку (только для первого запуска)
```

### Ошибка: "git pull failed"

**Решение:**
```bash
# На сервере настройте git
cd /opt/neyroon-bot
git config --global --add safe.directory /opt/neyroon-bot
git config pull.rebase false
```

### Деплой долго выполняется

**Решение:** Используйте кеширование:
```yaml
- name: Cache Docker layers
  uses: actions/cache@v3
  with:
    path: /tmp/.buildx-cache
    key: ${{ runner.os }}-buildx-${{ github.sha }}
```

---

## Мониторинг деплоев

### В GitHub:
- **Actions** вкладка показывает историю всех деплоев
- **Insights** → **Deployments** показывает статистику

### В Telegram:
- Получайте уведомления о каждом деплое
- Видите коммит и автора
- Моментально узнаете об ошибках

---

## Альтернативные варианты

### Вариант 1: Webhook на сервере

Создайте endpoint на сервере, который будет слушать GitHub webhooks и автоматически обновляться.

### Вариант 2: Watchtower

Автоматически обновляет Docker контейнеры при появлении новых образов:
```yaml
watchtower:
  image: containrrr/watchtower
  volumes:
    - /var/run/docker.sock:/var/run/docker.sock
  command: --interval 300  # Проверка каждые 5 минут
```

### Вариант 3: GitLab CI/CD

Если используете GitLab вместо GitHub, можете использовать `.gitlab-ci.yml`

---

## Полезные команды

```bash
# Просмотр истории деплоев на сервере
cd /opt/neyroon-bot
git log --oneline -10

# Откат к предыдущей версии
git reset --hard HEAD~1
docker-compose -f docker-compose.prod.yml up -d --build

# Просмотр текущей версии
git rev-parse --short HEAD
```

---

**Готово!** Теперь при каждом `git push` ваш проект будет автоматически развертываться на сервере! 🚀

---

## Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [SSH Action](https://github.com/appleboy/ssh-action)
- [Telegram Action](https://github.com/appleboy/telegram-action)
