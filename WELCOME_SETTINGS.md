# Настройка приветственного сообщения

## Обзор

Система позволяет полностью настроить приветственное сообщение, которое видят новые пользователи при первом запуске бота `/start`.

## ✨ Возможности

- **📸 Фото приветствия** - добавьте изображение для визуального представления курса
- **📝 Текст приветствия** - настраиваемое описание с поддержкой Markdown
- **🔘 Кнопки выбора уровня** - кастомные тексты для кнопок "Новичок" и "Уже знаком"
- **👁️ Превью в реальном времени** - видите как будет выглядеть сообщение

## 🎯 Как настроить

### Через админ панель:

1. Откройте админ панель: `http://localhost:3000/admin/`
2. Авторизуйтесь с помощью `ADMIN_SECRET` токена
3. Перейдите на вкладку **"⚙️ Приветствие"**
4. Настройте:
   - **Фото** - загрузите изображение (JPG, PNG, GIF, WebP, до 5 МБ)
   - **Текст** - введите описание курса с Markdown форматированием
   - **Кнопки** - измените текст кнопок (по умолчанию "Новичок" и "Уже знаком")
5. Нажмите **"💾 Сохранить изменения"**

### Через API:

```bash
# Получить текущие настройки
curl -H "Authorization: Bearer YOUR_ADMIN_SECRET" \
  http://localhost:3000/api/settings/welcome

# Обновить настройки
curl -X PUT \
  -H "Authorization: Bearer YOUR_ADMIN_SECRET" \
  -H "Content-Type: application/json" \
  -d '{
    "welcomeText": "🎨 **Добро пожаловать!**\n\nВаш текст здесь...",
    "welcomeButtonNewbie": "Я новичок",
    "welcomeButtonExperienced": "Уже умею рисовать"
  }' \
  http://localhost:3000/api/settings/welcome

# Загрузить фото
curl -X POST \
  -H "Authorization: Bearer YOUR_ADMIN_SECRET" \
  -F "photo=@/path/to/image.jpg" \
  http://localhost:3000/api/settings/welcome/photo

# Удалить фото
curl -X DELETE \
  -H "Authorization: Bearer YOUR_ADMIN_SECRET" \
  http://localhost:3000/api/settings/welcome/photo
```

## 📐 Структура настроек

```typescript
interface WelcomeSettings {
  welcomePhoto?: string;              // URL или путь к фото
  welcomeText?: string;               // Текст с Markdown
  welcomeButtonNewbie: string;        // Текст кнопки для новичков
  welcomeButtonExperienced: string;   // Текст кнопки для опытных
}
```

## 🎨 Markdown форматирование

В тексте приветствия поддерживается:

```markdown
**Жирный текст**
*Курсив*
`Код или выделение`
Обычный текст
```

Пример:
```markdown
🎨 **Добро пожаловать в Neyroon Bot!**

Это интерактивный курс рисования с помощью нейросетей.

📚 **Что вас ждет:**
• Ежедневные уроки
• Практические задания
• Обратная связь

Выберите ваш уровень опыта:
```

## 📸 Рекомендации по фото

### Размер и формат:
- **Максимальный размер:** 5 МБ
- **Форматы:** JPG, PNG, GIF, WebP
- **Рекомендуемое разрешение:** 1280x720 или 800x600
- **Соотношение сторон:** 16:9 или 4:3

### Содержание:
- Используйте яркое, привлекательное изображение
- Избегайте текста на изображении (он плохо читается)
- Фото должно отражать суть курса
- Не используйте слишком темные изображения

### Примеры подходящих фото:
- Логотип курса
- Примеры работ
- Промо-изображение
- Иллюстрация процесса обучения

## 🔧 Техническая реализация

### База данных

Настройки хранятся в таблице `BotSettings`:

```prisma
model BotSettings {
  id                        String   @id @default(cuid())
  key                       String   @unique
  welcomePhoto              String?
  welcomeText               String?
  welcomeButtonNewbie       String   @default("Новичок")
  welcomeButtonExperienced  String   @default("Уже знаком")
  createdAt                 DateTime @default(now())
  updatedAt                 DateTime @updatedAt
}
```

### Сервис

`src/services/botSettingsService.ts`:
- `getWelcomeSettings()` - получить настройки
- `updateWelcomeSettings()` - обновить настройки
- `removeWelcomePhoto()` - удалить фото

### API Endpoints

- `GET /api/settings/welcome` - получить настройки
- `PUT /api/settings/welcome` - обновить настройки
- `POST /api/settings/welcome/photo` - загрузить фото
- `DELETE /api/settings/welcome/photo` - удалить фото

### Бот

`src/bot/handlers/start.ts`:
- При `/start` загружает настройки из БД
- Отправляет фото (если есть) с текстом
- Создает кнопки с кастомными текстами

## 🎭 Пример приветствия

### С фото:
```
[ФОТО: Красивое изображение курса]

🎨 **Добро пожаловать в Neyroon Bot!**

Это интерактивный курс рисования, который поможет вам
освоить искусство создания нейронных изображений.

📚 **Что вас ждет:**
• Ежедневные видео-уроки
• Практические задания
• Обратная связь от преподавателя
• Доступ к эксклюзивным материалам

Выберите ваш уровень опыта:

[Кнопка: Новичок]
[Кнопка: Уже знаком]
```

### Без фото:
```
🎨 **Добро пожаловать в Neyroon Bot!**

[... текст ...]

[Кнопка: Новичок]
[Кнопка: Уже знаком]
```

## 🔄 Обновление настроек

### Настройки применяются:
- ✅ **Сразу** для новых пользователей
- ❌ **Не применяются** для существующих (они уже прошли онбординг)

### Для тестирования изменений:
1. Используйте тестовый аккаунт Telegram
2. ИЛИ используйте команду `/reset` (если добавлена)
3. ИЛИ очистите данные пользователя через админ панель

## 💾 Файлы фото

Загруженные фото сохраняются в:
```
/uploads/welcome/welcome-photo.jpg
```

При загрузке нового фото, старое перезаписывается.

## ⚠️ Важные моменты

1. **Первый запуск**: При первом обращении к API создаются настройки по умолчанию
2. **Markdown**: Telegram поддерживает ограниченный набор Markdown
3. **Фото в caption**: Максимум 1024 символа текста вместе с фото
4. **Локальные файлы**: Фото загружаются на сервер и отправляются через InputFile
5. **Ошибки**: Если фото не найдено или повреждено, отправляется только текст

## 🧪 Тестирование

### 1. Проверка API:
```bash
# Health check
curl http://localhost:3000/health

# Получить настройки
curl -H "Authorization: Bearer $ADMIN_SECRET" \
  http://localhost:3000/api/settings/welcome
```

### 2. Проверка в боте:
1. Создайте новый Telegram аккаунт (или используйте `/reset`)
2. Отправьте `/start` боту
3. Проверьте:
   - ✅ Фото отображается
   - ✅ Текст корректно форматирован
   - ✅ Кнопки имеют правильные тексты

### 3. Проверка админ панели:
1. Откройте вкладку "⚙️ Приветствие"
2. Загрузите тестовое фото
3. Измените текст
4. Сохраните
5. Проверьте превью

## 📊 Мониторинг

Логи бота показывают:
```
✅ Admin server running on http://0.0.0.0:3000
```

При ошибках с фото:
```
Error sending welcome photo: [детали ошибки]
```

## 🔐 Безопасность

- Все API endpoints требуют `ADMIN_SECRET` токена
- Загрузка файлов ограничена по размеру (5 МБ)
- Проверка типа файла (только изображения)
- Файлы сохраняются в безопасной директории

## 📚 Связанные файлы

- `prisma/schema.prisma` - модель BotSettings
- `src/services/botSettingsService.ts` - сервис работы с настройками
- `src/admin/server.ts` - API endpoints (строки 705-802)
- `src/bot/handlers/start.ts` - обработчик /start (строки 43-109)
- `admin-panel/index.html` - UI админ панели (строки 270-350, 1123-1291)

---

✅ **Система настройки приветствия готова к использованию!**
